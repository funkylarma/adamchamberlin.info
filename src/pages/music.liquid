---
layout: page.liquid
title: Music
subhead: Where words fail, music speaks
description: This page is a quick and dirty archive page of all my LastFM scrobbles.
permalink: "/music/"
---
<p>
  All data is taken from my <a href="https://www.last.fm/user/FunkyLarma">LastFM</a> account.
</p>

<h2>Recent Played</h2>
<ul>
  {% for track in lastfm.recentTracks %}
    <li class="track">
      <a href="{{ track.url }}">
        {{ track.data.title }}
      </a>
       - <time class="postlist-date dt-published" datetime="{{ track.date | dateHtmlString }}">{{ track.date | dateLongDate }}</time>
    </li>
  {% endfor %}
</ul>

<h2>Weekly Tracks</h2>
<ul>
  {% for track in lastfm.weeklyTracks %}
    <li class="track">
      <a href="{{ track.url }}">
          {{ track.data.title }} - {{ track.data.artist }}
      </a>
       - {{ track.data.playcount }} Play{% if track.data.playcount > 1 %}s{% endif %}
    </li>
  {% endfor %}
</ul>

<h2>Weekly Artists</h2>
<ul>
  {% for artist in lastfm.weeklyArtists %}
    <li class="artist">
      <a href="{{ artist.url }}">
        {{ artist.data.title }}
      </a>
       - {{ artist.data.playcount }} Play{% if artist.data.playcount > 1 %}s{% endif %}
    </li>
  {% endfor %}
</ul>
